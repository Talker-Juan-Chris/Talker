@model IEnumerable<Talker.Models.ApplicationUser>




@{
    ViewBag.Title = "Following";
}

<h2>Following</h2>

<table class="table">
    <tr>
        <th>
            User
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model.Where(x => x.UserName == "test@talker.com"))
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserName)
            </td>
                @*@if (item.User == User.Identity.Name)
                {
                    <a href="@Url.Action("Edit", "Talks", new { id = item.TalkID })">Edit</a>
                    <a href="@Url.Action("Delete", "Talks", new { id = item.TalkID })">Delete</a>

                }*@
        </tr>
    }

</table>



